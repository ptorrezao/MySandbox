apiVersion: v1
kind: Service
metadata:
  name: birrafe
  namespace: birra
  labels:
    app.kubernetes.io/name: birrafe
    app.kubernetes.io/part-of: birrafe
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app.kubernetes.io/name: birrafe
    app.kubernetes.io/part-of: birrafe
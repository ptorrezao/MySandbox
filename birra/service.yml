apiVersion: v1
kind: Service
metadata:
  name: birrafe
spec:
  externalTrafficPolicy: Cluster
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: birrafe
  sessionAffinity: None
  type: LoadBalancer
